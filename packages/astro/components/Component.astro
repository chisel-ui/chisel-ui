---
import { BaseTheme } from '../index.ts'

export interface ComponentProps {
	as?: string
	theme?: BaseTheme
	class?: string
	style?: string
	[key: string]: any
}

const { as: Tag = 'div', theme, ...props } = Astro.props as ComponentProps
---

<Tag {...props}>
	<slot />
</Tag>

---
import { uid } from '@chisel-ui/core'
import './Stack.css'
import Component from './Component.astro'
import type { Props as ComponentProps } from './Component.astro'

export interface Props extends ComponentProps {
	splitAfter?: number
}

const {
	splitAfter,
    id = uid(),
    ...props
} = Astro.props as Props



const inlineStyle = splitAfter && `
#${id}:only-child {
	block-size: 100%;
}

#{$id} > :nth-child(${splitAfter}) {
	margin-block-end: auto;
}
`
---

{inlineStyle && (
	<style set:html={inlineStyle}></style>
)}

<Component data-chisel-stack {id} {...props}>
    <slot />
</Component>